from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Matching WMATA route shapes to street segments from OpenStreetMap.',
    author='wmata-bp',
    license='MIT',
)
